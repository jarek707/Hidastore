<%= form_for(@elmtype) do |f| %>
  <% if @elmtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@elmtype.errors.count, "error") %> prohibited this elmtype from being saved:</h2>

      <ul>
      <% @elmtype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :flag %><br />
    <%= f.text_field :flag %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
