<%= form_for(@exposure) do |f| %>
  <% if @exposure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exposure.errors.count, "error") %> prohibited this exposure from being saved:</h2>

      <ul>
      <% @exposure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :flag %><br />
    <%= f.text_field :flag %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
