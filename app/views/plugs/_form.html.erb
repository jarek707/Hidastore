<%= form_for(@plug) do |f| %>
  <% if @plug.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plug.errors.count, "error") %> prohibited this plug from being saved:</h2>

      <ul>
      <% @plug.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Legend"%><br />
    <%= f.text_field :legend %>
  </div>

  <div class="field">
    <%= f.label "Table Name"%><br />
    <%= f.text_field :tab_name %>
  </div>
  <div class="field">
    <%= f.label "Configuration flags" %><br />
    <%= f.number_field :flags %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
